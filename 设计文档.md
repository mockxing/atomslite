# Atoms Lite Demo Design Document

Version: v2.0 (Updated)

---

# 1. 项目背景

## 1.1 Challenge

实现一个可运行的 Atoms Demo。

要求：
- 可在线访问
- 有真实交互
- 数据持久化
- 类似 Atoms 的产品体验
- 6~8 小时完成

评分重点：
- 完成度
- 工程思维
- 用户体验
- 创新
- 可交付

---

# 2. 产品定位

## 产品名称

Atoms Lite

一句话介绍

> 一个 Project Driven 的 AI Native Workspace，
> 用户通过自然语言持续构建 Web Application，
> AI 的执行过程可见，生成结果实时预览，并支持持续演化。

注意：

本 Demo 并不是 ChatGPT。

也不是 Cursor。

而是一个围绕 Project 的 AI Workspace。

产品理念：

```
Workspace
  ↓
Project
  ↓
Execution
  ↓
Artifact
  ↓
Preview
  ↓
Continue Building
```

---

# 3. MVP范围

## 包含

✅ Workspace（三栏布局：左 Execution + 中 Artifact + 右 Preview）
✅ Project（创建、打开、删除、继续开发）
✅ AI Build（Demo 模式 + 真实 AI API 双模式）
✅ Execution Timeline（实时 + 持久化）
✅ Artifact（文件查看器、可折叠、下载项目）
✅ Preview（iframe + srcdoc，浏览器风格顶栏）
✅ Persistence（执行记录、对话、生成物全部持久化）
✅ Continue Building（在已有应用基础上持续修改）

---

## 不做

❌ 多Agent
❌ Publish
❌ Marketplace
❌ Team Workspace
❌ 权限
❌ Deploy
❌ Artifact 文件编辑（仅查看 + 下载）

---

# 4. 用户流程

```
Home 首页
  ↓ 输入需求 或 点击快捷标签
New Project + 自动进入 Workspace
  ↓
AI Build（SSE 流式执行）
  ↓ Execution Timeline 实时更新
生成应用 → Preview 自动显示
  ↓
Continue Building（输入新需求，如 "Add dark mode"）
  ↓ AI 在现有代码基础上修改
自动保存（Project + Execution + Conversation + Artifact 持久化）
  ↓
关闭浏览器，重新进入 → 恢复所有内容
```

---

# 5. 页面设计

## Home 首页

作用：创建 Project + 管理已有项目

布局：

```
┌─────────────────────────────────────────┐
│  Atoms Lite                             │
├─────────────────────────────────────────┤
│                                         │
│     What do you want to build?          │
│     ┌──────────────────────┐ [Generate] │
│     │ A todo app...         │           │
│     └──────────────────────┘           │
│                                         │
│     [Todo App] [Dashboard]             │
│     [Landing Page] [CRM]               │
│                                         │
├─────────────────────────────────────────┤
│  Recent Projects                        │
│  ┌──────┐ ┌──────┐ ┌──────┐            │
│  │Todo  │ │Dash  │ │CRM   │            │
│  │App   │ │board │ │      │            │
│  └──────┘ └──────┘ └──────┘            │
└─────────────────────────────────────────┘
```

交互：
- 输入框输入需求 → 点击 Generate 或回车
- 快捷标签（Todo App / Dashboard / Landing Page / CRM）一键填充
- 项目卡片点击 → 进入 Workspace
- 项目卡片支持删除

---

## Workspace 工作区

Workspace 是整个产品核心。

三栏布局：

```
┌──────────┬─────────┬────────────────────┐
│ 左栏     │ 中栏    │ 右栏               │
│ (320px)  │ (220px) │ (flex: 1)          │
│          │ 可折叠  │                    │
├──────────┼─────────┼────────────────────┤
│ Execution│ Artifact│ 浏览器风格顶栏     │
│ Timeline │ 文件树  │ ● ● ●  index.html │
│          │         │                    │
│ ✓ Req... │ .atoms/ │ ┌────────────────┐ │
│ ✓ UI ... │  ├index │ │                │ │
│ ✓ Comp.. │  └v1    │ │   Preview      │ │
│ ✓ Style  │         │ │   (iframe)     │ │
│ ✓ Code   │ [搜索框]│ │                │ │
│ ✓ Build  │         │ │                │ │
│          │         │ └────────────────┘ │
│──────────│         │                    │
│Conversation│       │                    │
│ You: Add  │         │                    │
│ AI: Done  │         │                    │
│──────────│         │                    │
│ [输入框]  │[Download]│                   │
└──────────┴─────────┴────────────────────┘
```

### 左栏（320px）
- **Execution Timeline**：6步执行过程，实时更新，持久化恢复
- **Conversation**：用户 Prompt + AI Response，持久化恢复
- **Prompt 输入**：底部输入框，支持 Enter 发送，Continue Building

### 中栏（220px，可折叠）
- **Header**：Artifact 标题 + 折叠按钮 ◀
- **搜索框**：搜索文件名
- **文件树**：.atoms 文件夹 + 文件列表（点击切换预览）
- **Download Project**：底部下载按钮
- **折叠态**：仅显示 ▶ 展开按钮

### 右栏（flex: 1）
- **浏览器风格顶栏**：红黄绿圆点 + URL 栏显示当前文件名
- **Preview**：iframe + srcdoc 实时渲染

---

# 6. 功能设计

## Project

字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| title | String | 项目标题（AI 生成） |
| description | String | 项目描述 |
| status | Enum | CREATED / PLANNING / GENERATING / BUILDING / READY / FAILED |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

功能：
- 创建（自动，Generate 时创建）
- 打开（点击项目卡片）
- 删除（级联删除 Conversation + Artifact + Execution）
- 继续开发

---

## Conversation

保存：
- User Prompt
- AI Response
- Continue Prompt

重新进入 Project → 恢复 Conversation。

---

## Execution Timeline

不是聊天。而是 AI 执行过程。

步骤：

1. ✓ Requirement Analysis — 分析用户需求
2. ✓ UI Planning — 设计界面
3. ✓ Component Generation — 生成组件
4. ✓ Style Generation — 生成样式
5. ✓ Code Generation — AI 编写代码
6. ✓ Build Preview — 构建预览

每一步：
- 先显示 running（紫色转圈）
- 完成后显示 completed（绿色对勾）

持久化：所有步骤写入数据库，重新进入恢复 Timeline。

去重策略：加载时按 step 去重，只保留最终状态（completed 优先于 running）。

---

## Artifact

文件查看器（不做编辑）：

- .atoms 文件夹下展示所有生成文件
- 点击文件 → 切换 Preview 显示内容
- 文件后显示版本号（v1, v2...）
- 文件图标根据类型区分（HTML / CSS / JS / JSON）

下载：
- 点击 Download Project → 下载当前 index.html

---

## Preview

- iframe + srcdoc 实时渲染
- 浏览器风格顶栏（红黄绿圆点 + URL 栏）
- 生成结束自动显示应用
- 切换 Artifact 文件时 Preview 同步更新

---

## Continue Building

例如：

```
第一次：Create Todo App
第二次：Add dark mode
第三次：Add login page
```

AI 不是重新生成，而是在已有 Project 基础继续修改。

实现：将已有 Artifact 内容作为 `existing_code` 传给 AI，AI 在此基础上修改。

这是整个 Demo 最大亮点。

---

# 7. Runtime设计

整体流程：

```
User Prompt
  ↓
Create Project (CREATED)
  ↓
Planner → Requirement Analysis (PLANNING)
  ↓
Execution Queue (6 steps, SSE stream)
  ↓
Generator → Code Generation (GENERATING)
  ↓
Artifact Manager → Save to DB
  ↓
Preview → iframe render (BUILDING)
  ↓
Project Status → READY
  ↓
Persistence → All data saved
```

状态流转：

```
CREATED → PLANNING → GENERATING → BUILDING → READY
                                         ↓
                                      FAILED
```

SSE 事件类型：

| type | 说明 |
|------|------|
| execution | 执行步骤更新（step + status + message） |
| artifact | 生成物内容（filename + content） |
| project_update | 项目状态更新 |

---

# 8. 数据模型

## Project

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| title | String | 项目标题 |
| description | String (nullable) | 项目描述 |
| status | String | 项目状态 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

## Conversation

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| project_id | String (FK) | 关联项目 |
| role | String | user / assistant |
| content | String | 消息内容 |
| created_at | DateTime | 创建时间 |

## Artifact

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| project_id | String (FK) | 关联项目 |
| filename | String | 文件名 |
| content | Text | 文件内容 |
| version | Integer | 版本号 |
| created_at | DateTime | 创建时间 |

## Execution

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| project_id | String (FK) | 关联项目 |
| step | String | 步骤名称 |
| status | String | running / completed / failed |
| message | String | 步骤信息 |
| created_at | DateTime | 创建时间 |

---

# 9. 技术架构

```
前端: Next.js 14 + Tailwind CSS
  ↓ fetch / SSE
后端: FastAPI + SQLAlchemy (async) + SQLite
  ↓ httpx
AI 层: OpenAI Compatible API
  ↓
数据库: SQLite (aiosqlite)
  ↓
预览: iframe + srcdoc
```

LLM 配置（`backend/.env`）：

| 变量 | 说明 | 示例 |
|------|------|------|
| OPENAI_API_KEY | API 密钥 | sk-xxx |
| OPENAI_BASE_URL | API 地址 | https://api.deepseek.com/v1 |
| OPENAI_MODEL | 模型名称 | deepseek-chat |

支持所有 OpenAI Compatible API：DeepSeek、硅基流动、通义千问、Groq 等。
未配置 Key 时自动使用内置 Demo 模式。

部署方案：
- 前端：Vercel
- 后端：Railway

---

# 10. UI设计原则

不要像 ChatGPT。而是像 Workspace。

- 首页 → Project 为中心
- Workspace → Execution 可观察
- 右边 → Application 实时
- Execution → 不是聊天记录，是 Workflow
- Preview → 实时渲染
- Conversation → 只是辅助

视觉风格：
- 深紫主色调（#6366f1 / #4f46e5）
- 圆角卡片
- 轻量阴影
- 干净留白

---

# 11. Demo亮点

① Execution Timeline ★★★★★
相比聊天，展示 AI Workflow。实时更新 + 持久化恢复。

② Continue Building ★★★★★
持续修改，不是重新生成。AI 在已有代码基础上演化。

③ Project Persistence ★★★★★
关闭浏览器，重新进入，恢复所有内容（Execution + Conversation + Artifact）。

④ Artifact Driven ★★★★☆
独立文件树展示，不是聊天回复。支持下载项目。

⑤ Workspace First ★★★★☆
三栏布局，围绕 Project 组织，可折叠 Artifact 面板。

---

# 12. 项目文件结构

```
atom-demo/
├── README.md
├── 设计文档.md
├── homepage.png / homepage1.png / execute.png / execute-detail.png
├── backend/
│   ├── .env / .env.example
│   ├── requirements.txt
│   └── app/
│       ├── main.py              # FastAPI 入口 + CORS
│       ├── config.py            # 配置管理
│       ├── database.py          # 数据库连接
│       ├── models/
│       │   ├── models.py        # 4个 SQLAlchemy 模型
│       │   └── schemas.py       # Pydantic schemas
│       ├── routers/
│       │   ├── projects.py      # Project CRUD
│       │   ├── conversations.py # 对话 API
│       │   ├── artifacts.py     # Artifact API
│       │   ├── executions.py    # Execution API
│       │   └── build.py         # SSE 流式构建
│       └── services/
│           └── ai_service.py    # AI 调用 + Demo 模式 + 持久化
└── frontend/
    ├── .env.local / .env.example
    ├── package.json
    └── src/
        ├── lib/
        │   ├── api.ts           # API 客户端
        │   └── types.ts         # TypeScript 类型
        └── app/
            ├── layout.tsx
            ├── globals.css
            ├── page.tsx          # 首页
            └── workspace/
                └── [id]/
                    └── page.tsx  # 工作区（三栏布局）
```

---

# 13. 后续扩展

- Version — Artifact 版本对比
- Memory — AI 记忆项目上下文
- Publish — 一键发布应用
- Plugin — 插件系统
- MCP — Model Context Protocol 集成
- Team Workspace — 多人协作
- Multi Agent — 多 Agent 协作
- Marketplace — 应用市场

---

# 14. 总结

本 Demo 不追求完整复刻 Atoms。

而是提炼其产品哲学：

- Project First
- Workspace First
- Execution Visible
- Artifact Driven
- Continuous Building

最终目标：

在 6~8 小时内，完成一个真正可运行、可持续开发、可在线体验的 AI Native Workspace。
