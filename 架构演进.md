# Atoms Lite 架构演进

记录当前实现现状与目标架构（v2）的差距，以及演进计划。

---

# 1. 目标架构（v2）

来源：`atom-runtime-v2.md`

```
                    User Goal
                         │
                         ▼
             Requirement Analyzer
                         │
                         ▼
                  Goal Parser
                         │
                         ▼
                 Task Planner
                         │
                         ▼
               Task Graph Builder  ← 生成 Task DAG
                         │
                         ▼
             Task Orchestrator     ← 状态机 + 调度
                         │
      ┌─────────┼───────────┐
      ▼         ▼           ▼
 Architecture  Generator   Reviewer   ← Capability Agents
      │         │           │
      └─────────┴───────────┘
                    │
                    ▼
              Artifact Manager
                    │
                    ▼
               Preview Renderer
                    │
                    ▼
             Persistence Store
```

### 核心设计理念

> 本文件描述**目标架构（v2，来源 `atom-runtime-v2.md`）**。对照原文需明确两层：
> - **v2 目标 = Capability Agent**（见 v2 §6）：**非人格化**的能力节点（Requirement Analyzer / Planner / Architecture Designer / Code Generator / Code Reviewer / Preview Builder），由 Scheduler→Dispatcher 映射到具体 Capability；每个 Agent 完成一种能力，**并非**独立人格/记忆体。
> - **Multi-Agent（一个 Capability 映射多个真正 Agent）= v2 文档的 Future**（见 v2 §18），属于更远演进，不是 v2 目标本身。
>
> **当前实现**是单一 LLM 实例 + 多角色（prompt 模板）的串行流水线——Analyzer/Planner/Generator 等只是 `execute_llm_tool` 按 `prompt_type` 切换的 system prompt，共享同一客户端、不持有状态，既无 Capability Agent 节点隔离，也无 Dispatcher。

| 原则（v2 原文） | 说明 | 当前态 |
|------|------|--------|
| **Goal First** | 从用户输入提取 Goal，而非直接 Prompt → LLM（v2 §3） | ❌ 无 Goal Parser，直接走 Analyzer |
| **Task First** | 整个 Runtime 由 Task 驱动，不是由 Prompt 驱动（v2 §7） | ✅ 已实现（Task List 驱动逐步执行） |
| **Task DAG** | Task 之间有依赖关系，形成有向无环图（v2 §8） | ❌ 线性列表，deps 全空 |
| **Capability Agent** | 非人格化能力节点：Analyzer/Planner/Architect/Generator/Reviewer/Preview（v2 §6） | ⚠️ 当前为**多角色 Tool**（单 LLM + prompt_type 切换），无独立能力节点 |
| **Agent Dispatcher** | Scheduler→Dispatcher→Capability 映射，解耦调度与执行（v2 §10） | ❌ Scheduler 直接调用 Tool 函数 |
| **Task State Machine** | WAITING / READY / RUNNING / SUCCESS / FAILED / SKIPPED（v2 §7） | ❌ 仅 running/completed/failed |
| **DAG 演化** | Continue Building 在 DAG 上新增节点，不重新生成（v2 §8/§14） | ❌ 重新执行整个流程 |
| **Execution Visible** | Task Card 显示 Status / Duration / Output（v2 §13） | 🟡 Status/Detail 已有，缺 Duration/Output |

---

# 2. 当前实现现状

## 2.1 整体符合度：约 60%

一期已完成：真实的需求分析、动态任务规划、逐 Task 执行、多文件 Artifact（含过程文档）、Timeline 可点击展开、Markdown 渲染、HTML 后处理、Continue Building 修复等（详见 §6 里程碑）。

## 2.2 逐层对比

### Requirement Analyzer — 🟢 80%

**已实现**：
- ✅ 真实 LLM 调用，输出结构化 JSON：`app_type / core_features / ui_style / complexity / summary`
- ✅ Demo 模式模板匹配
- ✅ 结果用于下游 Task Planner 输入

**差距**：
- ⚠️ 缺少 Goal Parser 概念（Goal First 原则未体现）
- ⚠️ 没有需求 Diff（Continue Building 时对比已有需求与新需求）

### Task Planner — 🟡 55%

**已实现**：
- ✅ 真实 LLM 调用，动态生成 Task List（不再是固定模板）
- ✅ Task 带 name / tool / deps / params 字段
- ✅ 支持不同 prompt_type（analysis / design / generation / continuation）

**差距**：
- ❌ 生成的是**线性列表**，不是 **Task DAG**（deps 全部为空 `[]`）
- ❌ 没有 Task Graph Builder
- ❌ Continue Building 时重新执行整个流程，不在 DAG 上新增节点

### Scheduler / Orchestrator — 🟡 50%

**已实现**：
- ✅ 逐 Task 顺序调度执行
- ✅ 每个 Task 有真实 LLM 调用和产出
- ✅ 失败时更新项目状态为 FAILED

**差距**：
- ❌ 没有 **Task State Machine**（只有 running/completed/failed，缺 WAITING/READY/SKIPPED）
- ❌ 不支持**并行执行**就绪 Task
- ❌ 不支持**Retry**
- ❌ 不支持**优先级**
- ❌ 没有 **Agent Dispatcher** 层（Scheduler 直接调用 Tool 函数）

### Capability Agents（多角色）→ 目标：Capability Agent 节点 — 🟡 40%

> 口径说明：**当前实现是"多角色"而非"多 Agent"**。Analyzer / Planner / Generator / Docs 是 `execute_llm_tool` 中按 `prompt_type` 切换的不同 system prompt，共享同一 LLM 实例，不持有独立状态或记忆。
> 对照 v2 原文（§6）：**v2 目标是 Capability Agent——非人格化的能力节点**（Requirement Analyzer / Planner / Architecture Designer / Code Generator / Code Reviewer / Preview Builder），由 Scheduler→Dispatcher 映射；**Multi-Agent（一个 Capability 映射多个真正 Agent）属于 v2 的 Future（§18）**，不是 v2 目标本身。

**已实现（多角色 Tool）**：
- ✅ LLM 角色（按 prompt_type 区分 analysis / design / generation / continuation），单一 LLM 实例驱动
- ✅ File Writer Tool（保存多文件）
- ✅ Preview Tool（组装预览）
- ✅ Docs 角色（生成 architecture.md + progress.md，相当于轻量"过程文档"角色）

**差距（相对 v2 的 Capability Agent 目标）**：
- ❌ 没有 **独立能力节点隔离**（当前是 Tool 函数 + 切换 prompt，无 Agent/Dispatcher 边界）
- ❌ 没有 **Reviewer Agent**（Code Reviewer 能力节点，代码审查）
- ❌ 没有 **Architecture Designer Agent**（独立架构设计能力节点）
- ❌ 能力不可替换（不支持多模型、MCP Tool 注入，v2 §10 未来能力）

### Artifact Manager — 🟡 65%

**已实现**：
- ✅ 多文件生成（index.html + architecture.md + progress.md）
- ✅ 版本号管理（v1, v2...）
- ✅ 文件树展示，按文件名去重
- ✅ Markdown 渲染（.md 文件格式化预览）
- ✅ HTML 后处理（剥离 LLM 输出的 markdown 代码块和开场白）
- ✅ 增强提示词（具体设计规范：配色、字体、间距等）

**差距**：
- ❌ 代码仍是单文件（index.html 内嵌 CSS+JS），目标是多文件（Header.tsx / index.css 等）
- ❌ 没有 Artifact 间的依赖管理
- ❌ 增量更新能力弱（整个文件重写）

### Preview Renderer — 🟢 85%

**已实现**：
- ✅ iframe + srcdoc 渲染
- ✅ 浏览器风格顶栏
- ✅ Markdown 文件渲染
- ✅ 生成结束自动显示

**差距**：
- ⚠️ 全量替换而非增量刷新
- ⚠️ 多文件代码组合渲染未实现（当前只有单 HTML）

### Persistence Store — 🟡 65%

**已实现**：
- ✅ SQLite 四表：Project / Conversation / Execution / Artifact
- ✅ 执行记录、对话、生成物全部持久化
- ✅ 刷新页面恢复所有内容
- ✅ Execution 表含 task_order / task_type 字段

**差距**：
- ❌ Task 没有独立模型（用的是 Execution 表模拟）
- ❌ Task Graph / DAG 没有持久化
- ❌ 重新进入无法恢复 Task DAG 结构

### Execution UI — 🟡 55%

**已实现**：
- ✅ Task Card 可点击展开
- ✅ 显示 Status / Order / Detail / Tool 标签
- ✅ 折叠/展开交互
- ✅ 实时更新 Timeline

**差距**：
- ❌ 缺少 **Duration**（执行时长）展示
- ❌ 缺少 **Output**（产出摘要）展示
- ❌ 没有 **DAG 可视化**（React Flow）
- ❌ 线性列表，无法展示 Task 依赖关系

### Continue Building — 🟡 40%

**已实现**：
- ✅ 把已有代码作为 existing_code 传给 AI
- ✅ AI 在已有代码基础上增量修改
- ✅ 版本号递增

**差距**：
- ❌ 重新执行整个流程（Analysis → Planning → Generation），不是在 DAG 上新增节点
- ❌ 没有 Requirement Diff（需求差异分析）
- ❌ 没有部分重新生成（只改受影响的文件）

---

# 3. 差距总结

| 维度 | 当前 | 目标 v2 | 差距程度 |
|------|------|---------|----------|
| 需求分析 | ✅ LLM 结构化分析 | + Goal Parser + 需求 Diff | 🟡 中 |
| 任务规划 | 线性 Task List | Task DAG（带依赖） | 🔴 大 |
| 任务状态 | running/completed/failed | 6 态状态机 | 🔴 大 |
| 调度器 | 顺序执行 | DAG 拓扑 + 并行 + Retry | 🔴 大 |
| Agent | 多角色 Tool（单 LLM + prompt_type 切换） | Capability Agent（非人格化能力节点）+ Dispatcher | 🔴 大 |
| Reviewer | ❌ 无 | 代码审查 Agent | 🟡 中 |
| Artifact | 单 HTML + 2 md | 多文件代码（tsx/css/js） | 🟡 中 |
| 预览渲染 | ✅ iframe + md | + 多文件组合渲染 | 🟢 小 |
| 持久化 | 四表 | + Task/DAG 独立持久化 | 🟡 中 |
| Continue Building | 重新执行 | DAG 演化 + 增量 | 🔴 大 |
| Task Card UI | Status/Detail | + Duration/Output + DAG | 🟡 中 |
| **多租户/用户隔离** | **无用户体系，共享数据库** | **独立用户空间 + 数据隔离** | 🔴 大 |

### 多租户现状分析

当前所有项目数据存储在单个 SQLite 文件中，通过 `project_id` 做逻辑隔离：

| 维度 | 当前实现 | 问题 |
|------|----------|------|
| 存储 | 单 SQLite 文件（atoms_lite.db） | 所有用户共享同一数据库 |
| 隔离 | SQL `WHERE project_id = ?` 逻辑隔离 | 无用户隔离，任何人可访问/删除他人项目 |
| 文件 | Artifact 内容存在数据库 TEXT 字段 | 不是真实文件系统，无独立空间 |
| 认证 | ❌ 无 | 无用户身份标识 |

目标：每个用户开辟独立空间，项目数据物理或逻辑隔离。

---

# 4. 演进计划

## 二期 P0：Task DAG + 状态机（~4.5h）

目标：从线性 Task List 升级为 Task DAG，引入状态机，符合度 60% → 70%

| 任务 | 内容 | 预估 |
|------|------|------|
| Task 数据模型 | 新增 Task 模型（id/type/deps/status/input/output/duration/artifact_id），Task DAG 持久化 | 2h |
| Task Planner 升级 | Prompt 改为生成 Task DAG（带 deps 依赖关系），而非线性列表 | 1.5h |
| Task State Machine | 实现 WAITING / READY / RUNNING / SUCCESS / FAILED / SKIPPED 六态 | 1h |

## 二期 P1：多角色 → Capability Agent 升级 + 调度升级（~11h）

目标：将当前"多角色 Tool"升级为 v2 的 **Capability Agent**（非人格化能力节点 + Dispatcher），引入 Reviewer、DAG 调度、Continue Building 演化，符合度 70% → 85%。（注：Multi-Agent 多实例协作属 v2 Future，不在本期）

| 任务 | 内容 | 预估 |
|------|------|------|
| Scheduler 升级 | 基于 DAG 拓扑排序调度，支持并行就绪 Task | 2h |
| Agent Dispatcher | 从"直接调用 Tool 函数"升级为 Scheduler→Dispatcher→Capability 映射，分离 Architect/Generator/Reviewer 为独立能力节点 | 2h |
| Reviewer Agent | 新增 Code Reviewer 能力节点（代码审查），输出 Review Report | 1.5h |
| Task Card UI 升级 | 显示 Duration / Output，支持 DAG 可视化（React Flow） | 2.5h |
| Continue Building DAG 演化 | 在现有 DAG 上新增节点，不重新生成整个项目 | 2h |
| Task Graph 持久化与恢复 | 重新进入恢复 Task DAG 结构 | 1h |

## 二期 P2：多文件代码 + 增强（~5h）

目标：多文件代码生成，符合度 85% → 92%

| 任务 | 内容 | 预估 |
|------|------|------|
| 多文件代码生成 | 生成 index.html + styles.css + app.js 分离文件（或 Header.tsx 等组件） | 3h |
| React Flow DAG 可视化 | Task DAG 图形化展示，节点可点击 | 2h |

## 二期 P3：多租户改造 + 独立空间（~11.5h）

目标：用户体系 + 数据隔离 + 独立工作空间，从单租户 Demo 升级为多租户架构

### 设计方案

```
用户首次访问 → 输入昵称注册 → 分配 user_id → 开辟独立空间
                                              ↓
                                    /workspaces/{user_id}/
                                    ├── project_1/
                                    │   ├── index.html
                                    │   ├── styles.css
                                    │   └── app.js
                                    └── project_2/
                                        └── ...
```

| 层级 | 设计 |
|------|------|
| **认证** | 轻量方案：昵称 + JWT token，存 localStorage |
| **数据库隔离** | 所有表新增 `user_id` 字段，查询时按 user_id 过滤 |
| **文件空间** | 每用户一目录 `/workspaces/{user_id}/{project_id}/`，Artifact 存真实文件 |
| **项目隔离** | API 层校验：用户只能访问自己的项目和文件 |

### 任务拆解

| 任务 | 内容 | 预估 |
|------|------|------|
| 用户体系 | User 表 + 注册/登录 API + JWT token 生成与校验 | 3h |
| 数据库迁移 | 所有表新增 user_id 字段 + 索引 + 数据迁移脚本 | 1.5h |
| 后端 API 改造 | 所有查询加 user_id 过滤 + 权限校验中间件 | 2h |
| 文件系统空间 | Artifact 存真实文件（/workspaces/{user_id}/{project_id}/）+ 数据库只存元数据 | 2h |
| 前端适配 | 登录弹窗 + token 管理 + API 请求带 Authorization header | 2h |
| 联调测试 | 端到端验证多租户隔离 | 1h |

## 联调+测试

| 任务 | 预估 |
|------|------|
| 端到端验证 + 调试（P0-P2） | 2h |

## 总计

| 阶段 | 预估 |
|------|------|
| P0（核心 DAG + 状态机） | ~4.5h |
| P1（Agent + 调度 + 演化） | ~11h |
| P2（多文件 + 可视化） | ~5h |
| P3（多租户 + 独立空间） | ~11.5h |
| 联调测试 | ~2h |
| **总计** | **~34h** |

---

# 5. 风险点

1. **Task DAG 生成稳定性** — LLM 生成的依赖关系可能不合理（循环依赖、孤立节点），需要校验和容错
2. **并行执行复杂度** — 并行 Task 的 SSE 事件交错、共享 context 竞态、错误处理
3. **Reviewer Agent 价值** — 代码审查能否真正发现问题，还是只是形式上的额外步骤
4. **DAG 演化的上下文管理** — Continue Building 时如何确定哪些已有节点受影响、哪些需要新增
5. **多文件代码生成格式** — LLM 一次输出多个文件，格式一致性难保证（之前已遇到 markdown 代码块问题）
6. **React Flow 集成** — DAG 可视化的布局算法、交互复杂度
7. **多租户数据迁移** — 现有数据无 user_id，迁移时需要分配默认用户，且文件系统空间需同步创建
8. **文件系统空间并发** — 多用户同时构建时文件 I/O 并发，SQLite 写锁竞争加剧，可能需要迁移到 PostgreSQL

---

# 6. 已完成里程碑

| 日期 | 里程碑 | 符合度 |
|------|--------|--------|
| 2026-08-06 | 一期：真实执行流（Analyzer + Planner + 逐 Task 执行） | 35% → 55% |
| 2026-08-06 | 过程件（architecture.md + progress.md） | +5% |
| 2026-08-07 | Timeline 可点击展开 + N 悬浮框隐藏 | +2% |
| 2026-08-07 | Markdown 渲染 + HTML 后处理 + 提示词增强 | +3% |
| 2026-08-07 | Artifact 重复修复（SSE/loadData 竞态） | — |
| 2026-08-07 | Continue Building 修复（existing_code 取 index.html + continuation 类型 + 提示词增强） | — |
| 2026-08-07 | 预览默认显示 index.html | — |
| 2026-08-07 | 口径澄清（对照 v2 原文）：当前="多角色"；v2 目标=Capability Agent（非人格化能力节点）；Multi-Agent 属 v2 Future（§18） | — |

**当前符合度：~60%**

---

*文档更新时间：2026-08-07*
