# Atoms Lite — 产品功能及设计文档

> 版本：v1.0（基于当前线上实现整理，2026-08-07）
> 配套文档：设计文档.md（早期设计稿 v2.0）、issue.md（问题追踪）
> 线上环境：前端 https://atoms-lite-frontend.vercel.app ｜ 后端 https://atoms-lite-backend-production.up.railway.app

---

## 1. 产品概述

### 1.1 一句话定位

**Atoms Lite 是一个 Project-Driven 的 AI Native Workspace**：用户用自然语言描述需求，AI 实时编排执行并生成可交互的单文件 Web 应用，过程可见、结果可预览、支持持续迭代，所有上下文持久化。

它不是 ChatGPT（对话产品），也不是 Cursor（代码编辑器），而是围绕 **Project** 组织的 **AI 工作区**。

### 1.2 产品哲学

```
Workspace
  ↓  Project（以项目为中心组织一切）
  ↓  Execution（AI 执行过程可见、可观察）
  ↓  Artifact（产物独立管理，含代码与文档）
  ↓  Preview（实时渲染，所见即所得）
  ↓  Continue Building（在已有成果上持续演化）
```

核心关键词：**Project First / Execution Visible / Artifact Driven / Continuous Building**。

### 1.3 目标用户与场景

| 角色 | 场景 | 价值 |
|------|------|------|
| 业务人员 | "我要一个 todo app / 数据看板 / 落地页" | 0 代码生成可用应用 |
| 产品经理 | "给这个页面加个暗色模式" | 在现有成果上迭代，不重来 |
| 开发者（Demo/原型） | 快速出原型、验证想法 | 秒级预览、可下载单文件 |
| 评委/演示者 | 现场演示（优先有 Key 真实生成，无 Key 兜底） | 真实 AI 模式优先，Demo 模式作离线兜底 |

---

## 2. 核心功能（当前已实现）

### 2.1 AI Build — 多角色编排流水线

后端 `ai_service.py` 实现了一条真实的 **Analyzer → Planner → Scheduler → Tools** 流水线，**并非单一 prompt**。当前实现为**单一 LLM 实例 + 多角色（prompt 模板）**的串行编排：Analyzer / Planner / Generator 等"角色"只是 `execute_llm_tool` 中按 `prompt_type` 切换的不同 system prompt，共享同一个 LLM 客户端与模型，不持有独立状态或记忆。后续演进（对照 `atom-runtime-v2.md`）：v2 将升级为 **Capability Agent**（非人格化能力节点：Analyzer / Planner / Architect / Generator / Reviewer / Preview，由 Scheduler→Dispatcher 映射）；而 **Multi-Agent（一个 Capability 映射多个真正 Agent）属 v2 文档定义的更远 Future**。

```
用户 Prompt
  ↓
[Analyzer]   需求分析 → 结构化 JSON（app_type / core_features / ui_style / complexity）
  ↓
[Planner]    任务规划 → 4~7 个顺序 Task（tool ∈ {llm, file_writer, preview, docs}）
  ↓
[Scheduler]  顺序执行每个 Task：
  ├─ llm（analysis/design/generation/continuation）→ 调用 LLM 生成内容
  ├─ file_writer → 将生成物落库为 Artifact（带版本号）
  ├─ preview → 组装预览
  └─ docs → 生成 architecture.md + progress.md（过程文档）
  ↓
[Persistence] 全部写入数据库
```

每个 Task 通过 `execution` 事件实时上报状态（running → completed），前端 Timeline 逐步点亮。

**关键能力：**
- **生成（generation）**：输出单个自包含 HTML 文件（内联 CSS/JS，无外部依赖、无 CDN），符合 `GENERATOR_SYSTEM_PROMPT` 的视觉规范（indigo 主色 #6366f1、system fonts、圆角阴影、响应式、微交互）。
- **续写（continuation）**：传入 `existing_code`，AI 在已有代码基础上**增量修改**，不重写（见 §2.5）。
- **过程文档（docs）**：每次构建自动生成 `architecture.md`（设计哲学/组件结构/数据流/技术决策）与 `progress.md`（需求/用户故事/任务拆解/进度日志），作为 Artifact 一并保存。

### 2.2 Execution Timeline — 可观察的 AI 工作流

左栏实时展示 AI 执行步骤，**它不是聊天记录，而是 Workflow 可视化的核心**。

- 步骤由 Planner 动态生成（4~7 步，命名贴合具体应用，如 "Generate Todo App"），而非固定 6 步模板。
- 每步状态：`running`（紫色转圈）→ `completed`（绿色对勾），持久化到 `executions` 表。
- 重新进入 Project 时按 `(step, task_order)` 恢复，加载时按 filename 去重保留最新版本。

事件类型（SSE）：

| type | 说明 |
|------|------|
| `execution` | 执行步骤更新（step / status / message / task_order / task_type） |
| `plan` | 需求分析结果（analysis JSON） |
| `tasks` | Planner 生成的任务列表 |
| `artifact` | 产物内容（filename / content / version / **truncated**） |
| `project_update` | 项目状态变更（CREATED→PLANNING→GENERATING→BUILDING→READY） |

### 2.3 Artifact 产物管理

中栏是**独立的文件查看器**（查看 + 下载，不做编辑，符合 MVP 范围）：

- 展示当前 Project 所有生成文件（`index.html` 为主，外加 `architecture.md` / `progress.md` 等过程文档）。
- 点击文件名 → 右栏 Preview 同步切换显示该文件内容（HTML 渲染、Markdown 原文展示）。
- 文件名后显示版本号（v1, v2…），同一文件名多次生成按版本去重保留最新。
- **Download**：将当前激活的 Artifact 以 Blob 方式下载为本地文件（`handleDownload`）。

### 2.4 Preview — 实时沙箱预览

右栏是浏览器风格的预览区：

- `iframe` + `srcDoc` 实时渲染 HTML。
- 顶栏红黄绿圆点 + URL 栏显示当前文件名。
- 沙箱属性：`sandbox="allow-scripts allow-forms allow-modals allow-same-origin"`。
  - `allow-same-origin` 用于让生成应用内的 `localStorage` 等在同源沙箱内可用（已验证 todo app 交互恢复正常）。
  - 生成 HTML 已要求将所有 `localStorage` 访问包裹 `try/catch` 并降级为内存态，兼容沙箱限制。

### 2.5 Continue Building — 持续演化（产品最大亮点）

用户在工作区输入新需求（如 "Add dark mode" / "加个登录页"），AI **不重新生成**，而是：

1. 取出当前 `index.html` 作为 `existing_code`；
2. 走 `continuation` prompt（保留全部已有功能，仅增量修改）；
3. 新版本覆盖为同一文件的下一版本（v2, v3…），Timeline 与 Conversation 持续追加。

这使得一个 Project 可以沿用户意图逐步演化，而非每次从零开始。

### 2.6 Project Persistence — 全量持久化

关闭浏览器、重新进入，所有内容恢复如初：

| 数据 | 表 | 恢复方式 |
|------|----|---------|
| Project 元数据 | `projects` | 首页列表 / 工作区加载 |
| 对话 | `conversations` | 左栏 Conversation 恢复 |
| 执行步骤 | `executions` | 左栏 Timeline 恢复（去重） |
| 产物 | `artifacts` | 中栏文件树 + 预览恢复 |

状态机：`CREATED → PLANNING → GENERATING → BUILDING → READY`（异常时 `FAILED`）。

### 2.7 双模式：真实 AI + Demo

- **真实模式**：配置 `OPENAI_API_KEY` 后，调用任意 OpenAI Compatible API（DeepSeek / 通义千问 / Kimi / 硅基流动 / Groq 等），模型由 `OPENAI_MODEL` 指定（当前为自定义配置，如 deepseek-v4-flash / qwen3.7 / kimi2.6）。
- **Demo 模式**：未配置 Key 时自动启用，内置 Todo / Dashboard / Landing / CRM 模板，**完全离线**，适合无网现场演示。Continue Building 在 Demo 模式下同样支持（如添加 Dark Mode）。

### 2.8 多模型故障转移（MODEL_POOL）

为提升真实模式在线上（尤其 Railway 美节点跨太平洋）的**可用性**，后端支持通过 `MODEL_POOL` 环境变量配置**一组 LLM provider**，构建时按顺序尝试，遇故障自动切换到下一个，实现多模型热备。

- **配置方式**：`MODEL_POOL` 是一个 JSON 数组，每个元素是一个 provider：
  ```json
  [
    {"key": "sk-xxx", "base_url": "https://api.deepseek.com/v1", "model": "deepseek-v4-flash"},
    {"key": "sk-yyy", "base_url": "https://api.moonshot.cn/v1", "model": "kimi-k2.7-code"}
  ]
  ```
- **故障转移逻辑**：`ai_service.py` 的 `call_llm()` 依次遍历 pool 内每个 provider，任一 provider 出现**服务不可用（503）/ 限流（429）/ 超时 / 连接错误**时，自动切到下一个；只有全部 provider 都失败才抛出异常。每次调用 `AsyncOpenAI(max_retries=0)` 避免 SDK 默认重试放大超时（详见 Issue 2）。
- **统一入口**：流水线 5 处 LLM 调用（需求分析 / 任务规划 / 生成主调用 + fallback / 过程文档）全部经 `call_llm()`，故障转移对全流程生效。
- **向后兼容**：`MODEL_POOL` 为空（未配置）时，自动回退到单 `OPENAI_API_KEY / OPENAI_BASE_URL / OPENAI_MODEL` 配置，行为与旧版一致；两者皆无则进入 Demo 模式。
- **解析容错**：`config.py` 的 `model_pool` 解析失败（如全角逗号、`https:/` 少斜杠）时记录 `log.error` 告警并告警，不再静默退化走 Demo（详见 Issue 6）。
- **已知约束**：
  - 流式（`stream=True`）场景下，故障转移仅在 `create()` 抛错时触发；**流中途断流无法无缝切换到下一个 provider**（需重发请求）。
  - 故障转移只覆盖"provider 不可用"，不解决模型能力差异导致的质量问题。

> 说明：Railway 生产环境的 `MODEL_POOL` 变量值由用户自行管理，部署脚本不擅自改动。

### 2.9 生成完整性保护（最新修复）

长页面（大 HTML+CSS+JS）曾因 `max_tokens` 过小被硬截断，导致脚本未写完、页面无交互。当前已：

- 将 generation 的 `max_tokens` 由 16000 提升至 32000（自定义模型支持更大输出）。
- 截断检测：`</html>` 缺失时标记 `is_truncated=True`，并随 `artifact` 事件携带 `truncated` 字段下发前端。
- 前端在 `truncated` 为真时，于对话区提示"生成被截断，请重新生成"，避免用户误以为功能损坏。

> 说明：已生成的残缺页面不会自动修复，**需重新发起一次 Build** 才会以新的 `max_tokens` 生成完整内容。

---

## 3. 用户流程

```
Home 首页
  ↓ 输入需求 或 点击快捷标签
New Project → 自动进入 Workspace（带初始 prompt 触发首次构建）
  ↓
AI Build（SSE 流式执行，Analyzer→Planner→Scheduler→Tools）
  ↓ Execution Timeline 实时点亮、Artifact 同步落库
生成应用 → Preview 自动显示
  ↓
Continue Building（输入新需求，如 "Add dark mode"）
  ↓ AI 在现有代码基础上增量修改，版本 +1
自动保存（Project + Execution + Conversation + Artifact 全部持久化）
  ↓
关闭浏览器，重新进入 → 恢复所有内容（含历史版本）
  ↓
Download 当前激活 Artifact 为本地单文件
```

---

## 4. 技术架构（真实栈）

```
┌─────────────────────────────────────────────────────────────┐
│  前端  Next.js 16 + Tailwind + TypeScript（App Router）       │
│   /                      首页（需求输入 + 项目列表）          │
│   /workspace/[id]        工作区（三栏：Exec/Artifact/Preview）│
│   /bff/[...path]         BFF 反向代理（见 §4.3）              │
└───────────────┬───────────────────────────┬─────────────────┘
                │ fetch / SSE                 │ /bff/* → /api/*
                ▼                             ▼
┌─────────────────────────────────────────────────────────────┐
│  后端  FastAPI + SQLAlchemy(async) + SQLite(aiosqlite)        │
│   /api/projects         Project CRUD                          │
│   /api/conversations    对话记录                              │
│   /api/artifacts        产物管理（版本去重）                  │
│   /api/executions       执行步骤（持久化）                    │
│   /api/build/stream     SSE 流式构建（核心）                  │
│   app/services/ai_service.py  Analyzer/Planner/Scheduler/Tools │
└───────────────┬───────────────────────────┬─────────────────┘
                │ httpx (OpenAI Compatible)  │ aiosqlite
                ▼                             ▼
        ┌──────────────────┐          ┌──────────────────┐
        │  LLM Provider    │          │  SQLite Database  │
        │ (DeepSeek/Qwen/  │          │  (Railway Volume │
        │  Kimi/Groq...)   │          │   /data 持久化)   │
        └──────────────────┘          └──────────────────┘
```

### 4.1 后端

- **框架**：FastAPI（异步），`aiosqlite` 异步驱动。
- **AI 层**：`AsyncOpenAI` 客户端，兼容任意 OpenAI 协议端点。`call_llm()` 统一封装对 `MODEL_POOL`（多 provider 数组，按序故障转移）或单 `OPENAI_*` 配置（向后兼容）的调用，每次调用 `max_retries=0` 避免超时放大（详见 §2.8、Issue 2）。
- **流水线**：`stream_build_process` 为 `AsyncGenerator`，逐 Task yield SSE 事件，并在每个阶段 `await db.commit()` 持久化。
- **Tools**：`execute_llm_tool`（含截断检测，返回 `(content, is_truncated)`）、`execute_file_writer_tool`（落库 + 版本管理）、`execute_preview_tool`（组装）、`generate_docs`（过程文档）。

### 4.2 前端

- **框架**：Next.js 16（App Router）+ Tailwind + TypeScript。
- **状态**：Workspace 页以 React `useState` 管理 Timeline / Artifact / Preview / Conversation。
- **流式**：`streamBuild()` 通过 `fetch` + `ReadableStream` 解析 SSE，按事件类型驱动 UI。
- **预览**：`getPreviewContent()` 处理当前 Artifact 内容（HTML 走 srcDoc，Markdown 走原文展示）。

### 4.3 BFF 代理层（规避 CORS）

前端不直接跨域调后端，而是通过 Vercel 的 `/bff/[...path]` Route Handler 将请求反代到 Railway 后端：

```
/bff/projects  →  {BACKEND_URL}/api/projects
```

由 `BACKEND_URL` 环境变量指向 Railway 后端地址。优点：同源部署、无需 CORS 配置、简化前端联调。

### 4.4 部署架构

| 层 | 平台 | 说明 |
|----|------|------|
| 前端 | Vercel | `frontend` 目录，`npm run build`，环境变量 `BACKEND_URL` |
| 后端 | Railway | `backend` 目录，`railway up --detach`，挂载 `/data` Volume 持久化 DB |
| 数据库 | SQLite on Volume | `DATABASE_URL=sqlite+aiosqlite:////data/atoms_lite.db` |

部署命令（SOP）：
- 后端：`cd backend && railway up --detach`
- 前端：`cd frontend && vercel deploy --prod --yes`

---

## 5. 数据模型

| 表 | 关键字段 | 说明 |
|----|---------|------|
| `projects` | id(UUID), title, description, status, created_at, updated_at | 项目主体，状态机驱动 |
| `conversations` | id, project_id(FK), role(user/assistant), content, created_at | 对话历史 |
| `executions` | id, project_id(FK), step, status(running/completed/failed), message, task_order, task_type, created_at | 执行步骤（可观察、可恢复） |
| `artifacts` | id, project_id(FK), filename, content(Text), version(Int), created_at | 产物（代码 + 过程文档，按 filename 去重保留最新版本） |

---

## 6. 设计原则与视觉

- **不像 ChatGPT，像 Workspace**：首页以 Project 为中心；工作区 Execution 可观察、Preview 实时、Conversation 仅辅助。
- **视觉规范**：深紫主色 `#6366f1` / `#4f46e5`，圆角卡片，轻量阴影，干净留白（与生成 HTML 的视觉语言一致）。
- **响应式**：生成应用要求 flexbox/grid 响应式、交互有 hover/active 态、含 empty state 与微交互。

---

## 7. 当前已知限制（透明披露）

| 限制 | 现状 | 影响 |
|------|------|------|
| 单文件产物 | 仅生成单个 `index.html`（+ 文档），不支持多文件工程 | 复杂应用受限 |
| 截断风险 | `max_tokens=32000`，超大页面仍可能截断并提示重生成 | 需手动重试 |
| 流式故障转移弱 | `stream=True` 模式下 MODEL_POOL 仅在建连 `create()` 抛错时切换；**流中途断流无法无缝切到下一 provider** | 需重发构建请求（见 §2.8） |
| 产物只读 | Artifact 仅查看/下载，不提供在线编辑 | 迭代只能靠 Continue Building |
| 无用户系统 | 无鉴权、无多租户 | 单人 Demo 形态 |
| 无发布/市场 | 不支持 Publish / 分享链接 / Marketplace | 生成物仅本地下载 |

---

## 8. 目标演进路径（Roadmap）

基于当前 v1 的真实能力与已知限制，规划分阶段演进。每条均呼应已识别的需求或问题。

### v1.1 — 生成健壮性（近期，已部分落地）
- ✅ `max_tokens` 提升至 32000 + 截断标记 + 前端提示。
- ⏳ **分块续写（Chunked Continuation）**：当单次生成逼近上限时，自动将"结构生成"与"内容填充"拆分为多轮 Task，彻底消除长页面截断（根治 Issue 7）。
- ⏳ Artifact 完整性校验：构建结束时校验 `</html>` 闭合，未闭合自动重试一次更长生成。

### v1.2 — 产物工程化（中期）
- **多文件工程**：支持 HTML/CSS/JS 分离、多页面路由，Artifact 树从"单文件"升级为"项目结构"。
- **在线编辑**：Artifact 查看器增加可编辑模式（CodeMirror），保存即新版本。
- **版本对比（Diff）**：同文件不同版本间可视化差异（呼应早期设计稿"后续扩展"）。

### v2.0 — 协作与生态（远期）
- **Memory**：AI 记忆项目上下文与用户偏好，跨会话保持一致性。
- **Publish**：一键发布生成应用为公开 URL（分享/演示），解决"无发布"限制。
- **Plugin & MCP**：插件系统 + Model Context Protocol 集成，扩展数据源与工具。
- **Team Workspace**：多人协作、权限、评论（呼应早期设计稿"不做"清单的延期项）。

### v3.0 — 平台化（愿景）
- **Multi-Agent 深度协作**：专用 Agent（设计/前端/测试/部署）并行与审校，提升复杂应用质量。
- **Marketplace**：用户发布模板与应用，形成生态。
- **评测与可观测性**：构建质量打分、失败归因、用户反馈闭环。

### 演进原则
1. **先治本后扩展**：生成完整性（v1.1）优先于功能堆砌。
2. **Artifact 为中心**：所有能力围绕"产物可管理、可演化、可发布"展开。
3. **演示友好**：始终保持 Demo 模式可离线运行，降低评审/现场门槛。
4. **兼容演进**：新能力不破坏现有 Project 数据模型与持久化。

---

## 9. 总结

Atoms Lite 当前已交付一个**真实可运行、多角色编排、过程可观察、可持久化、可持续迭代**的 AI Native Workspace。其核心差异化在于：把"AI 生成应用"从一次性对话，重构为"围绕 Project 的持续构建工作流"（底层为单一 LLM 实例驱动的多角色流水线，后续按 `atom-runtime-v2.md` 演进为 Capability Agent 能力节点架构；Multi-Agent 多实例协作为更远 Future）。

下一阶段重点是从"能生成"走向"生成得稳、管得住、发得出"——先以 v1.1 的分块续写根治长页面截断，再沿 v1.2→v2.0→v3.0 路径逐步工程化、协作化、平台化。
